## 21 — Simple Sorting

Write a function `bubble_sort(nums)` that returns a **new list** with the numbers sorted ascending.

### Notes
- Don't use `sorted()` or `.sort()` for this exercise.
- A simple bubble sort is fine.
