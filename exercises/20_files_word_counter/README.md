## 20 — Files (Word Counter)

Write a function `count_words(text)` that returns how many words are in the string.

For this exercise:
- A "word" is anything separated by whitespace.
- Use `split()`.

Then write a function `count_words_in_file(path)` that reads a file and returns the word count.

A sample file is provided: `sample.txt`.
