## 20 — Nested Loops (Matrix)

Write a function `make_matrix(rows, cols)` that returns a matrix (list of lists)
filled with increasing integers starting at 1.

Example: `make_matrix(2, 3)` ->
```
[
  [1, 2, 3],
  [4, 5, 6]
]
```
