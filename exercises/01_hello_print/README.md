## 01 — Hello, Print

Write a function `say_hello()` that prints exactly:

Hello, world!

(Include the exclamation mark.)

### Requirements
- Use `print()`
- Function should return `None` (normal for print functions)
