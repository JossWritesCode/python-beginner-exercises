## 10 — Palindrome

Write a function `is_palindrome(s)` that returns `True` if `s` reads the same forwards and backwards.

### Notes
- Treat the string exactly as given (case-sensitive, include spaces/punctuation).
