## 18 — Anagrams

Write a function `are_anagrams(a, b)` that returns `True` if `a` and `b` are anagrams.

### Notes
- For this beginner version:
  - Treat strings as **case-sensitive**
  - Include spaces/punctuation exactly as written
- Hint: you can compare character counts (like the last exercise).
