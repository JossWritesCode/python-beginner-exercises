## 08 — Functions (Add Tax)

Write a function `add_tax(price, tax_rate)` that returns the final price.

- `price` is a number (int or float)
- `tax_rate` is a decimal like `0.13` for 13%

Return `price * (1 + tax_rate)`.

### Examples
- `add_tax(10, 0.13)` -> `11.3`
