## 11 — Lists (Sum and Average)

Write two functions:

- `sum_list(nums)` -> returns the sum of the numbers
- `average_list(nums)` -> returns the average (sum divided by count)

Assume `nums` is a non-empty list of numbers.
